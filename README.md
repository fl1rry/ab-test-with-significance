# ab-test-with-significance
This project is an A/B testing analysis tool built with BigQuery SQL, Python, and Tableau.  The goal is to evaluate whether the differences between control and test groups are statistically significant for key funnel metrics. The analysis is performed both at the overall test level and across separate user segments.
